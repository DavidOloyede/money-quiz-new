import type { LandingActions } from './Landing'
/** Landing section stub: hero. Replaced by the design-loop builder. */
export function LandingHero(_actions: LandingActions) {
  return <section id="landing-hero" className="py-24 text-center">Hero</section>
}
