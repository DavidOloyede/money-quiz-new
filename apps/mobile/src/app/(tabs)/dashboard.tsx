import { Card, Note, Screen } from '@/components/ui'

export default function DashboardScreen() {
  return (
    <Screen title="Dashboard">
      <Card>
        <Note>This screen arrives in this phase — building it next.</Note>
      </Card>
    </Screen>
  )
}
