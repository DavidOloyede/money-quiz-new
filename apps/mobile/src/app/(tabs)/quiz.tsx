import { Card, Note, Screen } from '@/components/ui'

export default function QuizScreen() {
  return (
    <Screen title="Quiz">
      <Card>
        <Note>The quiz arrives in this phase — building it next.</Note>
      </Card>
    </Screen>
  )
}
